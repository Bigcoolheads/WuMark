# WuMark (无码) - 极简Markdown编辑器 设计与开发文档

## 1. 项目概述

### 1.1 项目名称
- **英文名**: WuMark
- **中文名**: 无码
- **寓意**: 无干扰、极简的 Markdown 编辑体验

### 1.2 项目目标
开发一款类似 Typora 的所见即所得（WYSIWYG）Markdown 编辑器，打包为 Windows 可执行文件（.exe），可作为 .md 文件的默认打开方式。

### 1.3 技术选型
| 技术 | 用途 |
|------|------|
| **Electron 33** | 跨平台桌面框架 |
| **Node.js 24** | 运行时 |
| **CodeMirror 5** | Markdown 编辑内核与实时视觉渲染 |
| **marked 15** | Markdown 解析渲染 |
| **KaTeX** | 行内和块级数学公式渲染 |
| **node-emoji** | GitHub 风格 emoji 短码转换 |
| **highlight.js 11** | 代码语法高亮 |
| **electron-builder 25** | 打包为 exe |

### 1.4 开发者
- **作者**: wushaozhi
- **协议**: MIT License

## 2. 功能需求

### 2.1 核心功能
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 双栏编辑 | 左侧源码编辑，右侧实时预览 | P0 |
| 实时预览编辑 | 在同一编辑区直接显示标题、强调、引用、列表、链接和代码样式，同时保留 Markdown 原文 | P0 |
| 视图切换 | 实时预览/编辑/预览/双栏 四种模式 | P1 |
| 文件操作 | 新建/打开/保存/另存为 .md 文件 | P0 |
| 拖拽打开 | 拖拽 .md 文件到窗口直接打开 | P1 |
| 图片拖拽 | 拖拽图片到编辑器，自动保存并插入 Markdown 链接 | P1 |
| 图片粘贴 | 截图或剪贴板图片粘贴，保存为独立文件并插入 | P1 |
| 关闭提示 | 未保存时弹出自定义确认对话框（保存/丢弃/取消） | P1 |
| 自动保存 | 文件菜单中勾选开启，每 30 秒自动保存 | P2 |
| 语法高亮 | 预览中代码块语法高亮 | P0 |
| 工具栏 | 加粗/斜体/标题/列表/表格等快捷按钮 | P1 |
| 滑动同步 | 编辑与预览区滚动联动 | P1 |
| 查找与替换 | 文档内全文搜索、匹配导航、逐项/全部替换 | P0 |
| 文档大纲 | 提取标题层级，点击跳转并高亮当前章节 | P1 |
| 表格操作 | 单元格悬停定位当前行列，支持行前/行后、列左/列右插入及精准删除 | P1 |
| 文档统计 | 全文字符、中文、拉丁单词统计，多行选区显示局部统计 | P1 |
| Logo 主题 | 浅色与深色模式统一采用深空蓝 `#0F172A` 和青色 `#22D3EE` | P1 |
| 多格式导出 | 将当前渲染文档导出为独立 HTML、PDF 或 PNG 长图 | P1 |
| 语法帮助 | 在帮助菜单中展示语法示例与兼容范围 | P1 |

### 2.2 支持的Markdown语法
- **标题**: `#` ~ `######`
- **文本样式**: `**粗体**`, `*斜体*`, `~~删除线~~`, `` `行内代码` ``
- **列表**: 有序列表 / 无序列表 / 任务列表 `- [ ]`
- **链接**: `[text](url)`
- **图片**: `![alt](src)`
- **代码块**: 带语言标注的围栏代码块 ` ```lang `
- **表格**: 标准 Markdown 表格（支持内联格式）
- **引用**: `>` 块引用
- **分割线**: `---`, `***`
- **Emoji**: Unicode emoji 与 `:smile:` 风格短码
- **数学公式**: `$E = mc^2$` 与 `$$...$$` KaTeX 公式
- **HTML 与转义**: CommonMark/GFM 内联 HTML、反斜杠转义和自动链接

当前兼容目标为 **CommonMark + GFM + emoji + KaTeX**。脚注、定义列表、Mermaid 等属于额外 Markdown 方言，当前不解析，并在应用内语法帮助中明确标注。

### 2.3 界面布局
```
+------------------------------------------+
| 标题栏: [文件名] - WuMark                 |
+------------------------------------------+
| 菜单栏: [文件] [编辑] [视图] [帮助]       |
+------------------------------------------+
| 标签栏: [笔记1.md ✕] [笔记2.md ✕] [+]
+------------------------------------------+
| 工具栏: [B] [I] [S] [H1] [Code] [Table]  |
+------------------------------------------+
| 编辑区 (左)          | 预览区 (右)        |
| ┌──────────────────┐ | ┌────────────────┐ |
| │ # 标题           │ | │ 标题           │ |
| │ **粗体** text    │ | │ 粗体 text      │ |
| │ `code`           │ | │ code           │ |
| └──────────────────┘ | └────────────────┘ |
+------------------------------------------+
| 状态栏: [行:1 列:1] [单词:50] [A]        |
+------------------------------------------+
```

## 3. 系统架构

### 3.1 进程架构
```
┌──────────────────────────────┐
│   Main Process (main.js)      │
│  - 窗口管理                   │
│  - 文件 I/O                   │
│  - 原生菜单                   │
│  - 命令行参数处理             │
└──────────────┬───────────────┘
               │ IPC (ipcMain/ipcRenderer)
┌──────────────▼───────────────┐
│  Renderer Process             │
│  ├── index.html (界面骨架)    │
│  ├── app.js (应用逻辑)        │
│  ├── parser.js (Markdown解析) │
│  ├── editor.css (编辑器样式)  │
│  └── markdown.css (预览样式)  │
└──────────────────────────────┘
```

### 3.2 模块说明
| 文件 | 职责 |
|------|------|
| `main.js` | 窗口创建、菜单、文件IO、IPC处理、图片读写 |
| `preload.js` | 预加载脚本（contextBridge，已定义 API 但当前未使用） |
| `src/index.html` | 界面骨架 |
| `src/renderer/app.js` | 编辑器、查找替换、大纲、表格操作、视图切换、IPC、拖拽/粘贴 |
| `src/renderer/parser.js` | marked、KaTeX、emoji、代码高亮、标题与表格源定位 |
| `src/styles/editor.css` | 编辑区、工具栏、状态栏样式 |
| `src/styles/markdown.css` | Typora 风格预览样式 |
| `test/smoke.js` | Electron 端到端验证预览、公式、emoji、搜索、大纲、表格和多标签 |

### 3.3 统计口径

- **字符**: Unicode 字符总数，包含空格和换行
- **中文**: Unicode Han Script 汉字数量
- **单词**: 拉丁字母单词数量，可包含数字及单词内连字符
- 选中跨行文本时，编辑器在选区末端显示局部的行数、字符、中文和单词统计

## 4. 数据结构

### 4.1 应用状态
```javascript
// 应用状态（多标签）
const state = {
  tabs: [],               // 标签页数组
  activeTabId: null,      // 当前激活标签 ID
  viewMode: 'split',      // 'live' | 'edit' | 'preview' | 'split'
  autoSave: false,        // 自动保存开关
  autoSaveTimer: null,    // 自动保存定时器句柄
};

// 每个标签页结构
tab = {
  id: 'tab_xxx',          // 唯一 ID
  filePath: null,         // 文件路径
  fileName: '未命名.md', // 文件名
  content: '',            // Markdown 原文
  savedContent: '',       // 上次保存时的内容快照
  modified: false,        // 是否已修改
  scrollTop: 0,           // 编辑区滚动位置
  cursorPos: 0,           // 光标位置
};
```

### 4.2 IPC通信
| 通道 | 方向 | 说明 |
|------|------|------|
| `dialog:saveFile` | R→M | 保存文件 |
| `dialog:saveAs` | R→M | 另存为 |
| `file:opened` | M→R | 通知渲染进程文件已打开 |
| `file:saveClipboardImage` | R→M | 保存剪贴板图片到磁盘 |
| `file:saveDroppedImage` | R→M | 保存拖拽的图片文件到磁盘 |
| `dialog:confirmClose` | R→M | 关闭前确认对话框（自定义模态） |
| `menu:*` | M→R | 菜单操作事件 |
| `autoSave:enable` | R→M | 开启自动保存 |
| `autoSave:disable` | R→M | 关闭自动保存 |
| `window:setTitle` | M→R | 更新窗口标题（含当前文件名） |
| `tabs:switch` | R→M | 切换到指定标签页（恢复滚动/光标） |

### 4.3 多标签页

- 每个标签页独立维护 `filePath`、`content`、`savedContent`、`modified`、`scrollTop`、`cursorPos`
- 新建标签调用 `newFile()` 创建；关闭前检查 `modified` 状态，未保存则弹出关闭确认
- 打开文件时检测是否已在其他标签打开（按 `filePath` 比对），若已存在则直接切换到该标签
- 文件保存 / 另存为均针对当前激活标签
- 关闭窗口前遍历所有标签，收集所有未保存文件，统一提示
- 自动保存针对当前激活标签
  
### 4.4 图片处理策略

| 来源 | 检测方式 | 处理方式 | 保存路径 |
|------|----------|----------|----------|
| Explorer 拖拽 | `drop` 事件 + 扩展名检测 | 读取源文件 → 写入目标目录 | 当前 .md 同目录（未保存→桌面） |
| 截图粘贴 | `paste` 事件 + `clipboard.readImage()` | PNG 编码 → 写入磁盘 | 同上 |
| 手动输入 URL | marked 自定义 `renderer.image` | 直接渲染（绝对路径） | N/A |

- 图片文件名自动编号: `image-001.png`, `image-002.png` …
- 所有图片路径在渲染时通过 `encodeURI()` 处理，确保特殊字符（空格、中文等）正常加载
- CSP 已配置 `img-src 'self' data: file:` 允许本地 `file://` 协议

### 4.5 关闭确认对话框

关闭窗口前触发 `beforeunload`，主进程发 `dialog:confirmClose` 给渲染进程。渲染进程创建自定义模态弹窗（CSS 与编辑器风格统一），提供三个按钮：
- **保存** → 触发保存操作后关闭
- **丢弃** → 不保存直接关闭
- **取消** → 阻止关闭

### 4.6 自动保存

- 文件菜单中的复选框控制开关（`state.autoSave`）
- 开启后每 30 秒通过 `setInterval` 自动调用 `saveCurrent()`
- 关闭时清除定时器
- 状态栏显示 `[A]` 标识自动保存开启

## 5. 打包部署

### 5.1 构建命令
```powershell
# 设置国内镜像（网络原因）
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 打包为便携版 exe
npx electron-builder --win portable
```

### 5.2 输出文件
- `build/WuMark.exe` — 单文件便携版

### 5.3 文件关联
打包配置已注册 `.md` 文件关联，安装/运行后可将 WuMark 设置为 Markdown 文件的默认打开方式。

## 6. 开源说明
- **仓库**: https://github.com/Bigcoolheads/WuMark.git
- **协议**: MIT License
- **开发环境**: Node.js 24 + Electron 33
- **克隆后运行**: `npm install` → `npm start`
